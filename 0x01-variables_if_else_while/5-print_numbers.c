#include <stdio.h>
#include <stdlio.h>
#include <time.h>
/**
 * main - a program printing single digit numbers 0-9
 * Return: Always 0
 */
int main(void)
{
	int c;
	for (c = 0; c < 10; c++);
	{
		printf("%d", c);
	}
	printf('\n');
	return (0);
}

