#include <stdio.h>
/**
 * main - main block
 * Description: print all single digit number of base 10
 * starting from 0, followed by new line.
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%d"; i);
	printf("\n');

	return (0);
}
